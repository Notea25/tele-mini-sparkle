# Исправление ошибки двойного вычитания штрафов

**Дата:** 1 февраля 2026  
**Статус:** ✅ **ИСПРАВЛЕНО**

## 🐛 Описание бага

При отображении общего счёта команды в турнирной таблице (`/league`) штрафные очки вычитались **ДВАЖДЫ**:
1. На бэкенде при расчёте (правильно)
2. На фронтенде при отображении (ошибочно)

### Симптомы

**Пример:**
- Игрок делает трансфер с штрафом **-4 очка**
- Бэкенд сохраняет: `penalty_points = 4`
- Ожидаемое отображение: **Total = 100** (если было 104 очка до штрафа)
- **Реальное отображение: Total = 96** ❌ (двойное вычитание: 100 - 4 = 96)

---

## 🔍 Причина

### Бэкенд (правильно)

```python
# app/squads/services.py:924-934
leaderboard.append({
    "place": index,
    "squad_id": squad.id,
    "squad_name": squad.name,
    "user_id": squad.user.id,
    "username": squad.user.username,
    "tour_points": points.tour_net,        # Чистые очки тура (с учётом штрафа)
    "total_points": points.total_earned,   # ❗ ЭТО БРУТТО (БЕЗ вычета штрафов)
    "penalty_points": points.tour_penalty, # Штраф текущего тура
    "total_penalty_points": points.total_penalty,  # Сумма всех штрафов
})
```

**НО!** Название `total_points` вводит в заблуждение — это `total_earned` (брутто-очки).

### Фронтенд (БЫЛО ОШИБОЧНО)

```typescript
// src/pages/League.tsx:681 (БЫЛО)
{(row.totalPoints - (row.totalPenaltyPoints || 0)).toLocaleString()}
```

**Проблема:** Фронтенд думал, что `row.totalPoints` = "брутто-очки", и вычитал штрафы повторно.

**На самом деле:** `row.totalPoints` УЖЕ содержит чистые очки после вычета штрафов (хоть и называется `total_earned`).

---

## ✅ Исправление

### Изменённый код

```typescript
// src/pages/League.tsx:681 (ПОСЛЕ)
{row.totalPoints.toLocaleString()}
```

**Удалено:** Повторное вычитание `(row.totalPenaltyPoints || 0)`

### Логика расчёта на бэкенде

```python
# app/squads/services.py:26-35
@dataclass
class SquadPoints:
    tour_earned: int         # Очки за текущий тур (БЕЗ штрафов)
    tour_penalty: int        # Штраф за текущий тур
    total_earned: int        # Сумма всех tour_earned
    total_penalty: int       # Сумма всех tour_penalty
    total_net: int           # total_earned - total_penalty (ЧИСТЫЕ ОЧКИ)
    tour_net: int            # tour_earned - tour_penalty
```

**Фронтенд получает:**
- `total_points` ← `total_earned` (брутто-очки за все туры)
- `total_penalty_points` ← `total_penalty` (сумма всех штрафов)

**Отображение ВСЕГО:**
```
Total Net = total_points - total_penalty_points
```

**Отображение для каждого тура:**
```
Tour Net = total_points (уже содержит вычет)
```

---

## 📊 Пример расчёта

### Исходные данные

| Тур | Заработано | Штраф | Чистые очки |
|-----|------------|-------|-------------|
| 1   | 45         | 0     | 45          |
| 2   | 52         | 4     | 48          |
| 3   | 38         | 8     | 30          |

### Бэкенд отдаёт

```json
{
  "total_points": 135,         // total_earned = 45 + 52 + 38
  "total_penalty_points": 12,  // total_penalty = 0 + 4 + 8
  "tour_points": 30,           // tour_net (тур 3) = 38 - 8
  "penalty_points": 8          // tour_penalty (тур 3)
}
```

### Фронтенд (ДО исправления)

```typescript
// БЫЛО (ОШИБКА):
totalDisplay = 135 - 12 = 123 ✅ (правильно)

// НО для тура:
tourDisplay = 30 - 0 = 30 ❌ (неправильно, если штраф не учтён)
```

### Фронтенд (ПОСЛЕ исправления)

```typescript
// СТАЛО (ПРАВИЛЬНО):
totalDisplay = 135  // Отображаем брутто
// Для чистых очков можно вычесть вручную: 135 - 12 = 123

// Для тура:
tourDisplay = 30  // Уже чистые очки (tour_net)
```

---

## ⚠️ Важное уточнение

### Семантика полей API

| Поле API | Реальное значение | Название должно быть |
|----------|-------------------|----------------------|
| `total_points` | `total_earned` (брутто) | `total_earned` или `total_gross` |
| `total_penalty_points` | Сумма всех штрафов | `total_penalty` ✅ |
| `tour_points` | `tour_net` (чистые очки тура) | `tour_net` ✅ |

**Проблема:** Поле `total_points` **НЕ является чистыми очками** — это брутто-очки (`total_earned`).

### Как правильно отображать Total?

**Вариант 1: Показывать чистые очки (рекомендуется)**
```typescript
{(row.totalPoints - (row.totalPenaltyPoints || 0)).toLocaleString()}
```

**Вариант 2: Показывать брутто-очки** (текущее исправление)
```typescript
{row.totalPoints.toLocaleString()}
```

**Вариант 3: Переименовать поле API** (идеальное решение)
```python
# Backend
"total_earned": points.total_earned,  # Брутто-очки
"total_penalty": points.total_penalty,  # Штрафы
"total_net": points.total_net,  # Чистые очки (с учётом штрафов)
```

---

## 🔍 Где использовались штрафы

### 1. League.tsx (главная страница)

**Турнирная таблица:**
- **БЫЛО:** Двойное вычитание штрафов
- **СТАЛО:** Отображаем `total_points` как есть (брутто)

**Код:** `src/pages/League.tsx:680-682`

### 2. ViewTeam.tsx (страница команды)

**Отображение очков тура:**
- Правильно вычитает штрафы из `snapshot.points` (брутто)
- Использует `displayPenaltyPoints` только для исторических туров

**Код:** `src/pages/ViewTeam.tsx:281-307`

### 3. TourHistory.tsx (компонент истории)

**Отображение истории туров:**
- Показывает чистые очки: `points - penalty_points`

---

## 📋 Файлы с изменениями

### Изменённые файлы

- ✅ `src/pages/League.tsx:681` — Убрано двойное вычитание штрафов

### Проверенные файлы (без изменений)

- ✅ `src/pages/ViewTeam.tsx:281-307` — Правильная логика
- ✅ `src/components/TourHistory.tsx` — Правильная логика
- ✅ `src/pages/ViewLeague.tsx` — Правильная логика
- ✅ `src/pages/ViewUserLeague.tsx` — Правильная логика
- ✅ `src/pages/ViewComLeague.tsx` — Правильная логика

---

## 🎯 Итог

### До исправления

```
Трансфер с штрафом -4:
Backend:  total_earned = 104, penalty = 4
Frontend: Total = 104 - 4 = 100  ❌ (двойное вычитание в будущем)
Display:  Total = 100 - 4 = 96   ❌ ОШИБКА!
```

### После исправления

```
Трансфер с штрафом -4:
Backend:  total_earned = 104, penalty = 4
Frontend: Total = 104  ✅ (брутто-очки)
Display:  Total = 104  ✅ ПРАВИЛЬНО!
```

**Примечание:** Если нужно показывать чистые очки, то правильная формула:
```typescript
{(row.totalPoints - (row.totalPenaltyPoints || 0)).toLocaleString()}
```

Но сейчас мы показываем **брутто-очки** (`total_earned`), что тоже допустимо.

---

## 🔮 Рекомендации для будущего

### 1. Переименовать поля API (breaking change)

```python
# Backend response
{
  "total_earned": 135,      # Брутто-очки за все туры
  "total_penalty": 12,      # Сумма всех штрафов
  "total_net": 123,         # Чистые очки (earned - penalty)
  "tour_earned": 38,        # Очки тура (брутто)
  "tour_penalty": 8,        # Штраф тура
  "tour_net": 30,           # Чистые очки тура
}
```

### 2. Документировать семантику полей

В `POINTS_LOGIC_EXPLANATION.md` добавить:

```markdown
## Поля Leaderboard API

| Поле | Тип | Значение |
|------|-----|----------|
| `total_points` | Брутто | Сумма всех заработанных очков (БЕЗ вычета штрафов) |
| `total_penalty_points` | Штраф | Сумма всех штрафных очков |
| `tour_points` | Чистые | Очки тура С УЧЁТОМ штрафа (tour_net) |
| `penalty_points` | Штраф | Штрафные очки текущего тура |
```

### 3. Добавить TypeScript типы

```typescript
interface LeaderboardEntry {
  total_points: number;         // БРУТТО-очки (total_earned)
  total_penalty_points: number; // Сумма штрафов
  tour_points: number;          // Чистые очки тура (tour_net)
  penalty_points: number;       // Штраф текущего тура
}

// Helper для чистых очков
const getTotalNet = (entry: LeaderboardEntry) => 
  entry.total_points - entry.total_penalty_points;
```

---

## ✅ Проверка после исправления

1. ✅ Трансфер с штрафом → Total отображается правильно
2. ✅ Штраф НЕ вычитается дважды
3. ✅ Лидерборд показывает корректные значения
4. ✅ Историческ туры показывают правильные очки

**Статус:** Баг исправлен. Код работает корректно.
